{
  "name": "techquiz-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Root package.json for repo-level dev tooling only (husky, commitlint). The web app lives in ./web with its own package.json.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.15.9"
}
